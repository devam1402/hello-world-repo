name: Hello World

on:
  push:
    branches:
      - main

jobs:
  hello-job:
    runs-on: ubuntu-latest
    name: Echo text
    steps:
      - name: Run Echo Command
        run: echo "Hello, world!"
